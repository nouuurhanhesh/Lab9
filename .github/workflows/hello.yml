name: Hello Agile

on:
  push:
    branches: [ ayhaga ]

  pull_request:
    branches: [ main ]

jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello from GitHub Actions!"

# test comment